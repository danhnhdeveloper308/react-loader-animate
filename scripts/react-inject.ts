import * as React from 'react';

// Injected by esbuild when compiling the classic JSX transform. Keeping this
// in one build-only module avoids touching every loader while preserving
// runtime compatibility from React 16.14 through current React releases.
export { React };
